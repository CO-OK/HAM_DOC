# 会话引子：开新 session 时贴上去

---

## 用户背景

- **位置**：上海普陀区隆德小区（内中环之间，18-30 层板楼）
- **设备**：RTL-SDR（自述 v4，但图示为 V3 套装多功能偶极天线，型号待确认）
- **兴趣方向**：HAM 业余无线电、SDR 入门、上海本地频段监听
- **沟通偏好**：直接、技术向、喜欢表格、中文
- **当前状态**：SDR 新手，**已系统学习过** SDR 核心参数原理

## 已学懂的概念（不需要再讲）

- **Bandwidth**（带宽）：解调器取多宽 = ±半带宽。SDR++ 用"总宽度"约定
- **Squelch**（静噪）：音频出口的门，不丢信号。SDR++ 用噪声静噪，测音频高频噪声功率
- **Gain**（增益）：SDR 内部 RF 放大器，0-50 dB。位于 ADC 之前。**太高会削波（频谱肩膀）**
- **AGC**：**必须关**，否则 Squelch 失效
- **dB / dBm / dBFS**：对数刻度，Power 用 dBm（相对 1 mW），Gain 是比值
- **物理链路**：EM 波 → 感应电压 → 功率 → IQ → 解调 → 音频 → Squelch
- **关键公式**：P = 10^((dBm-30)/10) W，V = √(P×50)，FSPL = 20log(d)+20log(f)+32.45，N = -174+10log(BW)

## 4 份已建文档（/Users/quanwei1/HAM/）

1. `SDR++上海监听指南.md` — 频段和配置表（无 SDR++ 参数介绍）
2. `SDR-Bandwidth-概念与原理.md` — 带宽原理
3. `SDR-信号强度dB与Squelch原理.md` — dB 换算和 Squelch 链路
4. `SDR-Gain-原理与Squelch对比.md` — Gain 与 Squelch 对比

## 待办 / 未完成

1. **今晚试 ATC 接收**：121.600 MHz 虹桥地面，Mode=AM，BW=6kHz，Squelch 关，Gain 30 dB，关 AGC
2. **型号确认**：让用户拍 SDR 本体近照，确认是 V3 还是 V4
3. **天线升级方向**：ADS-B 需要 1090 MHz 专用天线 + 滤波器 + LNA
4. **HAM 中继**：上海 RepeaterBook 只有 3 个，70cm 段 439.6250/439.8250（Fusion 模式，BR4AA/BR4AD）
5. **未学但相关**：Mode（AM/FM/SSB）、AGC 原理、FFT 窗函数、Sample Rate、IF 滤波细节、削波细节

## 用户的认知层次

- ✅ 物理层理解（EM 波 → 感应电流 → 功率）
- ✅ 链路层理解（处理链、Squelch 位置、Gain 位置）
- ✅ 公式层理解（dB 换算、噪声底、距离衰减）
- ⚠️ 还没学的：ADC 量化、FFT 频谱原理、数字滤波具体实现

## 沟通风格

- 不啰嗦，喜欢直入主题
- 问得深、爱推理（"信号强弱如何定义"、"回过头来再看信号？"）
- 喜欢表格、ASCII 流程图、公式
- 接受较长回复，但**不喜欢重复**
- 叫他"smart ass"（用户设定）
