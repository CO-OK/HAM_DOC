# SDR 收听 AIS 上海船舶监听指南

> 整理自 Mavis 助手对话内容，覆盖：AIS 协议原理、上海资源、SDR++ 配置、AIS-Catcher 解码、OpenCPN 海图显示、天线方案、调参排错
>
> **前置阅读**：[SDR++上海监听指南](./SDR++上海监听指南.md)（基础参数概念本文不重复）

---

## 目录

1. [AIS 是什么](#ais-是什么)
2. [物理层与协议栈](#物理层与协议栈)
3. [上海 AIS 资源分布](#上海-ais-资源分布)
4. [完整接收链路](#完整接收链路)
5. [SDR++ 配置](#sdr-配置)
6. [AIS-Catcher 安装与配置](#ais-catcher-安装与配置)
7. [OpenCPN 海图与显示](#opencpn-海图与显示)
8. [天线方案（关键）](#天线方案关键)
9. [调参与排错](#调参与排错)
10. [进阶：上传 MarineTraffic](#进阶上传-marinetraffic)
11. [推荐路线](#推荐路线)

---

## AIS 是什么

**AIS（Automatic Identification System）** = 船舶自动识别系统。

IMO（国际海事组织）强制要求 300 总吨以上商船、全部客船都必须装。它会**自动**周期性广播本船信息：

| 字段 | 例子 | 含义 |
|------|------|------|
| MMSI | 412345678 | 船舶唯一 ID（类似手机号） |
| 船名 | "YANG SHAN PORT" | |
| 位置 | 31.3456°N, 121.5678°E | GPS 实时坐标 |
| 速度 | 12.3 节 | 对地速度 |
| 航向 | 087° | 当前 COG |
| 目的地 | "SHANGHAI" | 本航次终点 |
| 船长/船宽 | 180m / 28m | |
| 船舶类型 | 70 (Cargo) | IMO 分类 |
| 状态 | 0 (Under way using engine) | 航行/锚泊/靠泊 |
| ETA | 03-15 14:00 | 预计到达时间 |

**关键事实**：AIS 是**只收不取**的合法民用接收。在全球绝大多数国家（含中国），**单纯接收 AIS 不需要任何许可**，因为船只在公开广播信息。

---

## 物理层与协议栈

> 你已经学过 Bandwidth 和 Gain，这里从 AIS 角度串起来。

### 频段与频道

| 频道 | 频率 | 用途 |
|------|------|------|
| AIS 1 (Ch 87B) | **161.975 MHz** | 主用频道 A |
| AIS 2 (Ch 88B) | **162.025 MHz** | 主用频道 B |
| AIS 3 (Ch 75) | 156.775 MHz | 长距离（卫星+基站） |
| AIS 4 (Ch 76) | 156.825 MHz | 长距离（卫星+基站） |

- 船只在 AIS 1 和 AIS 2 **交替发射**（用 SOTDMA 协议避开冲突）
- 接收时**同时覆盖两个频道**，所以 SDR 中心频率设 **162.000 MHz**、带宽 ≥ 50 kHz
- 156.775 / 156.825 是"长距离"用的，平时船主要发 161.975 / 162.025

### 调制

| 项 | 值 | 解释 |
|----|----|----|
| 调制方式 | **GMSK** | Gaussian Minimum Shift Keying，FM 的一种 |
| 比特率 | **9600 bps** | 高速数据 |
| 频道带宽 | 25 kHz | 标称（实际信号占 12.5 kHz） |
| 频偏 | ±2400 Hz | 1 的 0.5 倍比特率 |
| 高斯 BT | 0.4 | 滤波器的"圆滑度"参数 |

**为什么 SDR++ 自己解不了**：
- SDR++ 自带 AM / FM / SSB 解调器，但**没有 GMSK 解调器**
- 9600 bps 速度也超过音频能承载的范围（即使走虚拟音频线）
- 所以必须**外挂专门解码软件**（AIS-Catcher）

### 链路层

- **SOTDMA**（Self-Organized Time Division Multiple Access）
- 每分钟分 2250 个时隙，每时隙 26.67 ms
- 船自动选时隙发报，**无需基站协调**
- 船速越快，发报越频繁（2-10 秒一次）

---

## 上海 AIS 资源分布

### 普陀区能收到的船

| 区域 | 距离 | 密度 | 备注 |
|------|------|------|------|
| **黄浦江中心段** | 1-3 km | ⭐⭐⭐⭐⭐ | 游船、货船、引航艇一晚上几十条 |
| 苏州河 | 0.5-1 km | ⭐⭐⭐ | 清淤船、游船 |
| 吴淞口 / 长江口 | 25 km | ⭐⭐⭐⭐ | 进出上海港的所有大船 |
| 洋山深水港 | 60 km | ⭐⭐ | 视距边缘，巨型集装箱船 |
| 杭州湾 | 40-80 km | ⭐ | 视距边缘 |

**预期效果**：
- 黄浦江上的船**一晚上 50-200 条**（取决于时段）
- 长江口**几十条**
- 凌晨 2-5 点最安静，白天最密集

### 验证方法

打开 [MarineTraffic.com](https://www.marinetraffic.com)，搜索你普陀区位置，看附近有多少 AIS 目标在动。颜色越深（红/橙）= 信号越密。

---

## 完整接收链路

```
   ┌─────────────────────────────────────────┐
   │  162 MHz 射频 (船台 VHF 12.5W)         │
   │  ↓                                      │
   │  天线 (建议垂直极化)                   │
   │  ↓                                      │
   │  RTL-SDR (V3/V4) → IQ 数据            │
   │  ↓ USB                                  │
   │  SDR++ (中心 162.000 MHz, BW 50 kHz)   │
   │  ↓ 共享内存 / TCP                       │
   │  AIS-Catcher (GMSK 解码 → NMEA 0183)   │
   │  ↓ TCP 10110                            │
   │  OpenCPN (海图 + 实时船位)             │
   │  ↓ 可选                                │
   │  MarineTraffic.com (上传共享)          │
   └─────────────────────────────────────────┘
```

**两种数据传递方式**：

| 方式 | 配置复杂度 | 延迟 | 推荐场景 |
|------|----------|------|---------|
| **共享内存** | 中 | 极低 | macOS 推荐，性能最好 |
| **TCP socket** | 低 | 低 | 跨机器调试 |
| SDR++ 音频输出 + 虚拟音频线 | 高 | 高 | **不推荐**，延迟大且不稳 |

> 💡 **最简方案**：不用 SDR++，**AIS-Catcher 直接接管 RTL-SDR**。但很多用户已经在跑 SDR++，所以本文保留两路并存的写法。

---

## SDR++ 配置

> 你在 SDR++ 里只负责**调谐和给原始 IQ**，不解码。

### 主面板设置

| 项 | 值 | 说明 |
|----|----|------|
| 中心频率 (Tune) | **162.000 MHz** | 落在两个 AIS 频道正中 |
| Sample Rate | 2.4 MSPS | 留余量（BIS 上限 3.2 MSPS） |
| Bandwidth | **50 kHz** | 同时覆盖 161.975 + 162.025 |
| Mode | RAW / NFM | 见下表 |
| Gain | 30-40 dB | 调到信号清晰即可，**注意肩膀** |
| RF AGC | **关** | 必须关 |
| Squelch | **关** | 解码在外部，不需要 squelch |
| FFT Size | 8192 | 看频谱更清楚 |

### Mode 选哪个？

| Mode | 何时用 |
|------|--------|
| **NFM** | 推荐。如果你只是把音频输出去给虚拟声卡解码 |
| **RAW** | 如果用 SDR++ 的 IQ 共享内存直接给 AIS-Catcher |

**如果用 AIS-Catcher 直接接管 RTL-SDR**（推荐）：
- SDR++ **完全不用开**
- AIS-Catcher 用 `-d` 参数直接接 RTL-SDR
- 详细见下节

---

## AIS-Catcher 安装与配置

> 作者 jvde，C++ 写的高性能 AIS 解码器。GitHub: <https://github.com/jvde-github/AIS-catcher>
>
> 这是 macOS 上**首选**的 AIS 解码器。

### 安装

**方法 1：Homebrew（推荐）**

```bash
brew install abcd567a/tap/ais-catcher
```

> ⚠️ 注意：tap 源是 `abcd567a/tap`，不要写错。

**方法 2：源码编译**

```bash
git clone https://github.com/jvde-github/AIS-catcher.git
cd AIS-catcher
mkdir build && cd build
cmake ..
make -j4
sudo make install
```

### 快速启动

**最简单命令**（直接接管 RTL-SDR）：

```bash
AIS-catcher -v 10
```

输出会显示：
- 找到的 RTL-SDR 设备
- 实际调谐频率
- 解码到的 NMEA 句子
- 每条船的 MMSI / 位置 / 速度

### 常用参数

| 参数 | 默认 | 说明 |
|------|------|------|
| `-d` | 0 | RTL-SDR 设备索引（多设备时用） |
| `-p` | 162000000 | 中心频率 Hz（默认 162 MHz，正好） |
| `-s` | 2400000 | 采样率 |
| `-a` | 28 | 增益 dB |
| `-v` | 0 | verbose 级别（10 看全部 NMEA） |
| `-o` | stdout | 输出 NMEA，可重定向到文件 |
| `-q` | 127 | 静噪门限（dB） |
| `-M` | 2 | 模式（2 = 实时 RTL-SDR） |
| `-N` | 0 | NMEA 输出端口（默认 0=关闭） |
| `-P` | 10110 | NMEA TCP 端口（给 OpenCPN 用） |

### 推荐命令

**直接接管 RTL-SDR + 输出 NMEA 到 TCP 10110**：

```bash
AIS-catcher -v 5 -a 30 -P 10110
```

OpenCPN 监听 `localhost:10110` 就能拿到 NMEA 流。

**记录到文件**（留档分析）：

```bash
AIS-catcher -v 5 -a 30 -o ~/HAM/ais_log_$(date +%Y%m%d).nmea
```

**与 SDR++ 配合**（SDR++ 输出共享内存给 AIS-Catcher）：
- 较复杂，macOS 上路径和 Linux 不一样
- **建议直接用 AIS-Catcher 接管，绕过 SDR++** 更简单

### 验证解码成功

成功的标志：
- 终端持续滚动 NMEA 句子
- 看到 `!AIVDM,1,1,,A,13HOI:0P0000VOHLCnHQKwvL05Ip,0*23` 这种行
- `decoded NMEA: X msgs/sec` 计数 > 0

如果一直 0 条船：
- 检查天线和频率
- 检查增益（先调到 30-40）
- 确认你附近确实有船（MarineTraffic.com 验证）

---

## OpenCPN 海图与显示

> OpenCPN = 开源海图导航软件，原生支持 AIS 叠加。

### 安装

```bash
brew install --cask opencpn
```

或者从 <https://opencpn.org> 下载 dmg。

### 配置

1. **下载海图**：
   - OpenCPN 自带的"World Map"很简陋
   - 推荐装 **CM93**（付费但质量高）或免费源：
     -  NOAA 公开海图（不含中国近海）
     - **OpenSeaMap**（开源，众包）
     - 国内船友群里流出的中国海图（不保证时效）
2. **添加 AIS 数据源**：
   - 设置 (Options) → Connections → Add Connection
   - 类型：**Network** (TCP)
   - 地址：`localhost` 端口：`10110`
   - 勾选 "Receive NMEA data"
3. **启用 AIS 显示**：
   - 工具栏点 AIS 按钮
   - 船舶目标会出现在海图上
4. **验证**：
   - AIS-Catcher 在跑
   - OpenCPN 列表里应该出现黄浦江上的船（MMSI、名字、距离、航向）
   - 地图上每条船显示为小三角，悬停看详情

### 看什么

- **目标列表**：所有收得到的船，按距离/速度排序
- **CPA / TCPA**：每条船多久到最近会遇点（防碰撞算法用）
- **警报**：设置危险距离（如 0.5 海里以内），有船进入就告警

---

## 天线方案（关键）

### 极化——最大的坑

| 极化 | 损失 | 说明 |
|------|------|------|
| 垂直极化 | 0 dB | 船用 VHF 天线标准 |
| 水平极化 | **-20 dB** | V3 偶极子默认方向 |
| 斜 45° | -3 dB | 折中 |

> ⚠️ **V3 偶极子默认是水平摆放的，损失 20 dB！** 这等于把船的信号削弱到 1/100。

**最低成本修复**：把偶极子**立起来**，两根振子**垂直**于地面。每臂 46 cm，总长 92 cm。

### 长度

| 类型 | 长度 | 增益 | 备注 |
|------|------|------|------|
| 1/4 波长单极 | **46 cm** | 2.15 dBi | 需要地网（地面/反射面） |
| 1/2 波长偶极 | 92 cm (每臂 46) | 2.15 dBi | 无需地网，**推荐** |
| 5/8 波长 | 116 cm | 3.5 dBi | 增益更高，方向略向上 |
| 商用船用玻璃钢天线 | 1.2-2.5 m | 3-6 dBi | 阳台/窗边架设 |

### 制作最简单的偶极子

```
        馈线 (50Ω 同轴)
            │
        ┌───┴───┐
        │       │
      46cm     46cm
        │       │
        ↓       ↓
       上      上
       (立      (立
       起来)    起来)
```

- 用铜线或铝管
- 两臂总长 = 半波长 = 92 cm
- **必须垂直**于地面
- 接 RTL-SDR 端用 50Ω 同轴电缆

### 商用天线选项

- **淘宝"船用 VHF 天线 162MHz"**：20-100 元
- **钻石 SRH-770 / SRH-815** 等宽带手持天线：能覆盖 VHF 全段，60-100 元
- **RTL-SDR 套机里的 2m/70cm 棒子**：原装的不一定 162 MHz 谐振

### 安装位置

| 位置 | 效果 |
|------|------|
| 阳台外、窗外 | ⭐⭐⭐⭐⭐ 最佳 |
| 窗边（玻璃内侧） | ⭐⭐⭐ 大楼玻璃会衰减 3-5 dB |
| 室内 | ⭐⭐ 衰减 10-20 dB |
| 楼顶 | ⭐⭐⭐⭐ 完美，但需要馈线长 |

---

## 调参与排错

### 快速检查清单

| 症状 | 可能原因 | 排查方法 |
|------|---------|----------|
| 完全没信号 | 天线/线缆没接好 | 短接收器看噪声底是否正常 |
| 解码率 < 1 msg/s | 增益过低 | 调到 30-40 dB 再看 |
| 解码率 < 1 msg/s | 极化不对 | 把天线立起来（垂直） |
| 频谱有肩膀 | 增益过高/有强信号 | 增益降 5 dB |
| 解码率很高但船很少 | 时间不对 | 凌晨 1-5 点黄浦江船少 |
| MarineTraffic 上有船但 SDR 没收到 | 视距被挡 | 把天线挪到窗边/阳台 |

### 频谱应该长什么样

正常情况：
- **底噪**应该在 -90 ~ -110 dB
- 161.975 / 162.025 MHz 位置有**偶发尖峰**（船发射时）
- 尖峰出现时，终端应该立刻输出一条 NMEA
- 稳定的连续信号 = 强本地干扰（少见）

### 增益调节

1. 开 SDR++ / AIS-Catcher，先 0 dB
2. 慢慢往上加，每加 5 dB 等 30 秒看解码数
3. **峰值**就是最佳增益（再加 5 dB 解码数会下降）
4. 上海信号强，通常 **25-35 dB** 足够

### 肩膀现象

如果频谱上信号峰旁边出现对称的"鼓包"：
- 说明 RTL-SDR 已经过载失真
- 降 5-10 dB 增益
- 离干扰源近（隔壁手机、WiFi 路由器）也会

---

## 进阶：上传 MarineTraffic

[MarineTraffic.com](https://www.marinetraffic.com) 是全球最大的 AIS 共享网络。

### 上传的好处
- 给全球海图社区贡献数据
- 你能解锁该网站的高级功能（部分免费）
- 看到自己站点在 MarineTraffic 上的表现

### 配置步骤

1. **注册账号**：[https://www.marinetraffic.com](https://www.marinetraffic.com)
2. **添加站点**：User → My Stations → Add Station
3. **获得 Upload Key**：每个站点的唯一字符串
4. **运行 AIS-Catcher 上传**：

```bash
AIS-catcher -v 5 -a 30 -P 10110 -U <你的_Upload_Key>
```

或者用 `-M` 参数 + 上传 URL：

```bash
AIS-catcher -M 2 -U <Upload_Key>
```

5. **MarineTraffic 后台** 几小时后就能看到你站点的数据

### 全球类似的网络

- **MarineTraffic.com**（最全）
- **VesselFinder.com**
- **AISHub.net**（开源中立）
- **ShipXplorer.com**

可以同时上传到多个网络（每个用独立 key）。

---

## 推荐路线

| 阶段 | 目标 | 操作 |
|------|------|------|
| **第一步** | 让 AIS-Catcher 解码出第一条 NMEA | 直接命令行跑 `AIS-catcher -v 10` |
| **第二步** | 验证天线 | 看信号条数，挪天线位置再比 |
| **第三步** | OpenCPN 显示 | 配 TCP 10110，看海图出船 |
| **第四步** | 上传 MarineTraffic | 社区贡献 |
| **第五步** | 双 SDR 并行 | 一个 1090 MHz ADS-B + 一个 162 MHz AIS |

### 立即可做的事

1. **打开 MarineTraffic** 看普陀区附近有多少船
2. **brew install abcd567a/tap/ais-catcher** 装解码器
3. **把 V3 偶极子立起来**（垂直极化）
4. **跑 `AIS-catcher -v 10`** 看输出
5. **装 OpenCPN** 配 TCP 10110
6. **晚上 8-10 点** 试（黄浦江最热闹）

---

## 参考资料

- **AIS-Catcher GitHub**：<https://github.com/jvde-github/AIS-catcher>
- **OpenCPN**：<https://opencpn.org>
- **MarineTraffic**：<https://www.marinetraffic.com>
- **AIS 协议标准**：ITU-R M.1371（5 段，物理/链路/传输/安全/应用）
- **AIS 频道详细**：<https://www.itu.int/en/itu-r/terrestrial/Pages/default.aspx>
- **中国海事局 AIS 规定**：<https://www.msa.gov.cn>

---

*本文档由 Mavis 整理，如有错误欢迎指正。*
