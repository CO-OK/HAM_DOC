# SDR 中的 Bandwidth：概念与原理

> 整理自 Mavis 助手对话内容，专门讲清楚 Bandwidth 是啥、怎么工作、跟其他参数的关系。

---

## 目录

1. [三个核心概念区分](#三个核心概念区分)
2. [Bandwidth 是什么](#bandwidth-是什么)
3. [频谱图上的直观理解](#频谱图上的直观理解)
4. [SDR 里的处理链路](#sdr-里的处理链路)
5. [关键约定：总宽度 vs 单边偏移](#关键约定总宽度-vs-单边偏移)
6. [跟采样率的关系](#跟采样率的关系)
7. [不同模式的标准带宽](#不同模式的标准带宽)
8. [实战调参指南](#实战调参指南)
9. [进阶：为什么是这些值](#进阶为什么是这些值)
10. [常见误区](#常见误区)

---

## 三个核心概念区分

| 概念 | 问的问题 | 单位 | 通俗比喻 |
|------|---------|------|---------|
| **Frequency（频率）** | **在哪？** | Hz | 房子的地址 |
| **Bandwidth（带宽）** | **多宽？** | Hz | 房子多大 |
| **Amplitude（幅度）** | **多强？** | dB | 房子多亮 |

**关键点**：Bandwidth 也是一个 Hz 数字，但它是衡量**一段范围的宽度**，不是某个具体位置。

---

## Bandwidth 是什么

**Bandwidth（带宽）= 一个信号在频谱上占据的宽度。**

任何"承载信息"的信号都不止一个频率。比如：

- 你的声音 = 300 Hz + 500 Hz + 1 kHz + 2 kHz + ... 一堆频率叠加
- 这堆频率的**总范围** = 你声音的带宽

当这个声音被调制到射频上：

```
射频 100 MHz  ┌──────────────┐
中心位置       ╱   携带你的声音   ╲
（载波）     │  (300 - 3000Hz)  │
            └──────────────────┘
            ← 这一段就是带宽 →
```

---

## 频谱图上的直观理解

打开 SDR++ 看频谱图（Spectrum），任何一个"信号凸起"都有三个属性：

```
   幅度 dB
   ↑
+20 |              ┌─────────────────┐
+10 |             ╱                   ╲
  0 |────────────╱                     ╲──────────  底噪
-10 |          ╱   一个 FM 广播信号      ╲
   |________╱_____________________________╲______→ 频率 Hz
          100.4M    100.5M    100.6M
                ↑              ↑
            带宽 = 200 kHz   中心频率 = 100.5 MHz
```

**1. 中心位置**（中间的数字 100.5 MHz）= **Frequency**
**2. 凸起的宽度**（左右多宽）= **Bandwidth**
**3. 凸起的高度**（相对底噪高多少 dB）= **Amplitude**

不同模式的"凸起"形状对比：

| 模式 | 带宽 | 形状 |
|------|------|------|
| WFM 广播 | 200 kHz | 宽得像个平台 |
| NFM 对讲 | 12.5 kHz | 像个小土包 |
| AM | 6 kHz | 像根针 |
| SSB | 2.4 kHz | 像根细针 |
| CW 电报 | 500 Hz | 像颗钉子 |

---

## SDR 里的处理链路

Bandwidth 控制的是链路中的**这个环节**：

```
射频信号 (RF)  →  数字采样 (IQ)  →  解调 (FM/AM/SSB)  →  音频 (Audio)  →  你的耳朵
                                          ↑
                                     Bandwidth
                                     在这里起作用
```

**它通过一个带通滤波器实现**：

- **通带（passband）**：保留的频率范围
- **阻带（stopband）**：滤掉的频率范围
- **过渡带**：渐变区

在频域上：

```
增益
1.0 |        ┌──────┐
    |       /        \
0.5 |      /          \
    |     /            \
0.0 |────/              \────
    0    6.25kHz     12.5kHz
        频率 →
```

---

## 关键约定：总宽度 vs 单边偏移

**大多数软件（包括 SDR++）用"总宽度"约定**：

> **Bandwidth = 200 kHz 时，截取的是中心频率 ±100 kHz（总跨度 200 kHz），不是 ±200 kHz（总 400 kHz）。**

常见换算：

| Bandwidth 设置 | 实际截取范围（中心 ± 半带宽） |
|---------------|--------------------------|
| 200 kHz | ±100 kHz |
| 12.5 kHz | ±6.25 kHz |
| 6 kHz | ±3 kHz |
| 2.4 kHz | ±1.2 kHz |
| 500 Hz | ±250 Hz |

⚠️ **有些老软件用"单边"约定**（设 200 就是 ±200，总 400）。看文档时要确认是哪种。

---

## 跟采样率的关系

这俩不是一回事，但容易混：

| 概念 | 是什么 | 典型值 |
|------|-------|--------|
| **Sample Rate（采样率）** | SDR 一次性抓多宽的频谱 | 2.4 MSPS（2400 kHz） |
| **Bandwidth（带宽）** | 解调器要听的频道宽度 | WFM 200 kHz |

**采样率是"窗户大小"，Bandwidth 是"在窗户里挑哪个频道听"。**

完整画面：

```
你 SDR 抓 2.4 MHz 宽的频谱（= ±1.2 MHz 范围）
        ↓
   这一大段里可能有几十个 FM 广播
        ↓
WFM 解调器用 200 kHz 滤波器"挖"出你选的那个频道
        ↓
   FM 解调 → 音频
```

---

## 不同模式的标准带宽

| 模式 | 占用带宽 | SDR++ 推荐值 | 主要场景 |
|------|---------|------------|---------|
| **WFM 调频广播** | ±75 kHz 频偏 | **200 kHz** | FM 电台 |
| **NFM 商用/对讲** | ±5 kHz 频偏 | **12.5 kHz** | 出租车、对讲、集群 |
| **NFM 老式** | ±5 kHz 频偏 | **25 kHz** | 老式中继 |
| **AM 调幅** | 6 kHz 单边带 | **6 kHz** | 中波、短波语音 |
| **AM 高保真** | 8 kHz 单边带 | **8 kHz** | 航空 ATC |
| **SSB 单边带** | 2.4-3 kHz | **2.4 kHz** | 短波通联、HAM |
| **CW 等幅电报** | 100-500 Hz | **500 Hz** | 摩尔斯电码 |

---

## 实战调参指南

**基本流程**：

1. 先设到推荐值（上表）
2. 然后微调

**判断方向**：

| 现象 | 调整 |
|------|------|
| 声音**发闷**、听不清细节 | 加大 BW（往 8 kHz / 15 kHz 推） |
| **噪声大**、有杂音/串台 | 减小 BW（往 4 kHz / 6 kHz 推） |
| 听 CW 报务时满是噪声 | 收到信号后**尽量调窄**（100-500 Hz） |

**反直觉的点**：

NFM 的 Bandwidth（12.5 kHz）比**音频本身的范围（3 kHz）宽**。这是因为：

- 这个滤波器其实放在**解调前**（叫 IF 滤波器）
- 目的是**只让这一个频道的信号进来**
- 防止隔壁频道的信号串进来（也叫**邻道抑制**）

所以严格说，**这个 Bandwidth 实际是"频道宽度"，不是"音频宽度"**。理解这一层后调参就不纠结了。

---

## 进阶：为什么是这些值

**WFM 200 kHz 的来源**：

FM 广播标准：
- 最大频偏：±75 kHz
- 最高调制频率（音频）：15 kHz
- 按 Carson's rule 估算：2 × (75 + 15) = **180 kHz**
- 实际频道间隔：**200 kHz**（留 20 kHz 保护带）

所以 200 kHz 既是**信号占用的最小宽度**，也是**国际频道间隔**。

**NFM 12.5 kHz 的来源**：

商用对讲机：
- 频偏：±2.5 kHz 或 ±5 kHz
- 音频：3 kHz
- 2 × (5 + 3) = **16 kHz**
- 实际频道间隔：早期 25 kHz，**现代 12.5 kHz**（更挤）

所以 12.5 kHz 是**现代窄带 FM 频道标准**。

**AM 6 kHz 的来源**：

调幅广播：
- 最高音频：4.5 kHz（高音质量）
- 单边带：4.5 kHz
- 上下边带加起来：9 kHz
- AM 频道间隔：9 kHz（中波） 或 10 kHz（短波）
- 解调 BW 设 6 kHz 取的是**单边带**，包含完整音频

---

## 常见误区

### ❌ "Bandwidth 越大越好"

错。**越大 = 噪声和邻道干扰越多**。只设到你需要的宽度。

### ❌ "Bandwidth 就是音频带宽"

不准确。是**频道带宽**，对 FM 来说它包含整个频道，对 AM 来说是单边带宽度。

### ❌ "NFM 12.5 kHz 听着比 WFM 200 kHz 质量差"

频率范围上看是这样，**但实际上**：
- WFM 200 kHz → 解调出 15 kHz 音频（高音好）
- NFM 12.5 kHz → 解调出 3 kHz 音频（只够说话）

这是**设计目标不同**，不是"质量差"。对讲机只关心"听得清说话"，不需要 HiFi。

### ❌ "设成 200 kHz 就能听到 ±200 kHz 的范围"

错。**200 kHz 是总宽度，实际是 ±100 kHz**。详情看前面"关键约定"那节。

### ❌ "Bandwidth 可以随便设"

不行。设太宽 → 串台。设太窄 → 信号被切掉，听不清。要按模式推荐值起步。

---

## 一句话总结

> **Frequency 是位置（在哪），Bandwidth 是宽度（多宽），Amplitude 是高度（多强）。**
>
> Bandwidth 决定你"想看到/听到多宽的范围"，**太宽 = 把噪声和邻道也放进来，太窄 = 切掉有用信息**。

---

*本文档由 Mavis 整理，如有错误欢迎指正。*
