# 业余无线电:电离层与短波传播

> 学习笔记 · 整理于 2026-07-18

短波(HF)能打全球,靠的是电离层。电离层对电波**同时具有反射和吸收两种作用**,而且这两件事发生在不同高度、对频率的依赖方向相反。本篇讲清楚:反射靠什么、吸收靠什么、怎么选频、以及它们背后统一的物理根源。

> 数学公式用 LaTeX 书写,请用支持公式渲染的 Markdown 预览器查看(VS Code Markdown 预览 / Typora / Obsidian 等)。

---

## 一、总图:反射和吸收发生在不同层

```
高度 ↑
 F 层(~200-400 km)  ← 主要"反射"层(电子密度最高,把波弹回地面)
 E 层(~110 km)
 D 层(~60-90 km)    ← 主要"吸收"层(白天出现,啃掉能量)
──────────────────── 地面
```

- **反射**主要靠上面的 **F 层**(电子密度最大)
- **吸收**主要发生在下面的 **D 层**(白天才有,夜里消失)

> 这解释了**短波夜里传得更远**:夜里 D 层消失,吸收没了,信号畅通。

---

## 二、反射:临界频率与最高可用频率(MUF)

### 核心:电子越密,能反射的频率越高

电离层能不能把波弹回来,取决于该层的**电子密度**。有一个**临界频率 $f_c$**——垂直打上去恰好能被反射的最高频率:

$$f_c \approx 9\sqrt{N_e}$$

| 符号 | 含义 | 单位 |
|------|------|------|
| $f_c$ | 临界频率(该层能垂直反射的最高频率) | Hz |
| $N_e$ | 电子密度(每立方米自由电子个数) | 个/m³ |

判据:

$$\begin{cases} f < f_c & \Rightarrow \text{波被反射回地面} \;\checkmark \\ f > f_c & \Rightarrow \text{波穿透电离层飞向太空} \;\times \end{cases}$$

> 这是"短波能打全球、VHF/UHF 只能视距"的根本原因:短波频率低于 $f_c$ 被弹回,VHF/UHF 高于 $f_c$ 直接穿出去。

### 斜射时:最高可用频率 MUF

实际通联电波是**斜着**打向电离层的,斜射能反射的频率比垂直更高,由**割线定律(Secant Law)**给出:

$$\text{MUF} = f_c \cdot \sec\theta = \frac{f_c}{\cos\theta}$$

- $\theta$ = 入射角(越贴地平线打,$\theta$ 越大,MUF 越高 → 能用更高频率打更远)
- **MUF(最高可用频率)**:这条链路当下能用的频率上限
- 与之对应还有 **LUF(最低可用频率)**:低于它信号被吸收殆尽

> 火腿选频黄金法则:**尽量贴近但不超过 MUF**(频率越高吸收越小),常取 MUF 的 80%~90% 作为"最佳工作频率 FOT"。

---

## 三、吸收:为什么、以及多强

电波穿过电离层(尤其 D 层)时,电子被电波推动振荡,又跟中性气体分子碰撞,把能量变成热——这就是**吸收**。吸收量正比于:

$$\text{吸收} \;\propto\; \frac{N_e \cdot \nu}{f^2}$$

| 符号 | 含义 |
|------|------|
| $N_e$ | 电子密度 |
| $\nu$ | 碰撞频率(D 层低空气密度大,碰撞多) |
| $f$ | 电波频率 |

**关键在那个 $f^2$**:

$$\text{吸收} \;\propto\; \frac{1}{f^2}$$

**吸收与频率平方成反比 → 频率越低,被吸收越狠。** 这条 $1/f^2$ 解释一大堆现象:

| 现象 | 用 $\text{吸收} \propto 1/f^2$ 解释 |
|------|-----------------------------------|
| 白天低频段(80m/40m)发闷、传不远 | 白天 D 层强、$N_e$ 大,低频 $f$ 小 → 吸收爆炸 |
| 同一时刻高频段(20m/15m)更活跃 | $f$ 大 → 吸收小,穿过 D 层到 F 层被反射 |
| 夜里低频段突然能打远 | D 层消失、$\nu$ 与 $N_e$ 骤降 → 吸收没了 |

---

## 四、合起来:选频是"夹缝求生"

反射和吸收对频率的要求**方向相反**,选频就是在两者之间找窗口:

```
频率低 ←──────────────────────────→ 频率高
吸收大(D层啃)              吸收小
     ↑                          ↑
   LUF 下限                MUF 上限(再高就穿透)
     └──── 可用窗口(选这里)──────┘
           越靠 MUF,吸收越小、信号越好
```

| | 反射(F 层) | 吸收(D 层) |
|---|------------|------------|
| 关键公式 | $f_c=9\sqrt{N_e}$,$\ \text{MUF}=f_c\sec\theta$ | $\text{吸收}\propto N_e\nu/f^2$ |
| 对频率 | $f$ 太**高**就穿透(反射失败) | $f$ 太**低**就被啃光(吸收失败) |
| 主导层 | F 层(高、密度大) | D 层(低、白天才有) |
| 给火腿的启示 | 频率别超 MUF | 频率别太低、白天尤忌 |

---

## 五、$9\sqrt{N_e}$ 里的"9"是怎么来的

$f_c \approx 9\sqrt{N_e}$ 是**经验简化式**,它由更基本的**等离子体频率**公式化简而来。

### 完整公式:等离子体频率

$$f_p = \frac{1}{2\pi}\sqrt{\frac{N_e\, e^2}{\varepsilon_0\, m_e}}$$

除 $N_e$ 外全是物理常数:

| 符号 | 含义 | 数值 |
|------|------|------|
| $e$ | 电子电荷 | $1.602\times10^{-19}$ C |
| $\varepsilon_0$ | 真空介电常数 | $8.854\times10^{-12}$ F/m |
| $m_e$ | 电子质量 | $9.109\times10^{-31}$ kg |
| $\pi$ | 圆周率 | 3.14159… |

### 化简:把常数合并

把 $N_e$ 单独提出,其余常数并成一个系数:

$$f_p = \underbrace{\frac{1}{2\pi}\sqrt{\frac{e^2}{\varepsilon_0 m_e}}}_{\text{常数}} \cdot \sqrt{N_e}$$

代入数值算那个常数:

$$\frac{1}{2\pi}\sqrt{\frac{(1.602\times10^{-19})^2}{(8.854\times10^{-12})(9.109\times10^{-31})}} \;\approx\; 8.98 \;\approx\; 9$$

于是得到那条好记的工程式:

$$\boxed{\,f_p \approx 9\sqrt{N_e}\,}$$

> 临界频率 $f_c$ 就是垂直入射时的等离子体频率,所以 $f_c \approx 9\sqrt{N_e}$。那个"9"不是凑的,是三个物理常数算出来的。

---

## 六、更深的根:反射和吸收是同一个量的两面

反射和吸收其实来自**同一个物理量**——电离层的**复折射率** $n$。它是个复数,一个数同时装了两件事:**实部管"能不能传、往哪拐(反射)",虚部管"传的时候衰减多少(吸收)"。** 下面一步步拆开。

### 6.1 折射率 $n$ 是干嘛的

折射率描述"波在介质里怎么走"。就像光学里玻璃 n≈1.5、水 n≈1.33 决定光**拐弯的程度**;电离层对电波也有折射率(忽略地磁场与碰撞的简化式):

$$n^2 = 1 - \frac{f_p^2}{f^2}$$

- $f$ = 你发的电波频率
- $f_p = 9\sqrt{N_e}$ = 那层电离层的等离子体频率(电子越密越大)

关键:这个 $n^2$ **可正可负**,取决于 $f$ 和 $f_p$ 谁大。

### 6.2 实部管反射:为什么 $n^2<0$ 会"弹回"

先看 $n^2$ 的正负:

| 情况 | $f_p^2/f^2$ | $n^2$ | 含义 |
|------|-------------|-------|------|
| $f > f_p$(频率高) | 小于 1 | **正数** | $n$ 是正常实数 → 波能穿过去 |
| $f < f_p$(频率低) | 大于 1 | **负数** | $n^2<0$ → $n$ 变成虚数 → 波传不了 |

**为什么"$n^2<0$ → 传不了 → 弹回"?**

波在介质里深入距离 $x$ 的传播,数学上带一个因子 $e^{i\,n\,k x}$:

- $n$ 是**实数**时:这是个**振荡**(正常往前走的波)
- $n$ 是**虚数**时($n^2<0$,记 $n=i\kappa$):因子变成 $e^{-\kappa k x}$ —— **指数衰减**,不再振荡

终端可读版(把上面两句画出来):

```
n 实数(f > fp):  ∿∿∿∿∿∿∿→     波往里钻,能穿过去
n 虚数(f < fp):  ╲__________    一进介质就迅速衰减,进不去
```

波"进不去"、能量又不能凭空消失(纯虚数 $n$ 不吸收),那只有一条路:**原路反射回去**。这就是电离层像镜子把低频波弹回地面的机制,正对应前面判据 $f<f_c$ 反射、$f>f_c$ 穿透($f_c$ 就是 $f_p$)。

> **实部管反射一句话**:$f<f_p$ → $n$ 变虚 → 波无法在电离层里传播 → 只能被弹回 = 反射。

### 6.3 关键澄清:纯虚 $n$ 幅度掉到零,为什么不算"吸收"?

6.2 里纯虚 $n$ 让波幅度沿 $x$ 掉到零,看着像"能量没了",但它是**反射**不是**吸收**。症结在于:**"衰减"和"吸收"是两回事。**

| 词 | 含义 | 能量去哪了 |
|----|------|-----------|
| **衰减** | 波幅度随深入距离变小 | 不一定损失,可能被**弹回去了** |
| **吸收** | 能量被介质**拿走变成热** | 真的没了(转成热能) |

纯虚 $n$:波幅度确实掉到零(衰减了),但能量**没变成热,而是全反射回去**——所以"衰减但不吸收"。

**为什么不生热?因为生热要靠碰撞 $\nu$,而纯虚 $n$ 来自 $\nu=0$ 的公式。** 对比两式:

$$\underbrace{n^2 = 1 - \frac{f_p^2}{f^2}}_{\text{无碰撞 }\nu=0\text{,给出纯虚 }n}\qquad\qquad \underbrace{n^2 = 1 - \frac{f_p^2}{f^2 - i f\nu}}_{\text{有碰撞 }\nu\neq0\text{,给出复数 }n}$$

左式里根本没有 $\nu$(假设电子自由振荡、不撞东西)→ 没有把能量变热的机制 → 不吸收。

**更本质的区分:波还能不能"往前走"。**

| $n$ 形态 | 传播因子 | 波的行为 | 结果 |
|---------|---------|---------|------|
| 纯虚($n=i\kappa$,**无实部**) | $e^{-\kappa kx}$,**没有振荡项** | 压根不往前传,贴着界面跌到零 | 全反射,**不生热** |
| 复数($n=n_{实}+i n_{虚}$) | $e^{i n_{实}kx}\cdot e^{-n_{虚}kx}$,**边走边衰** | 真往里走一段,沿途碰撞 | **吸收生热** |

```
纯虚 n:  █▇▆▅▃▂▁________     贴着界面就跌到0,没往里走 → 弹回,不生热
复数 n:  ∿∿∿⌒⌒‿‿·····___     真往里走了一段,一路撞一路生热 → 吸收
```

关键:纯虚 $n$ **没有振荡项(实部为 0)**,波不"往前走",也就**没有沿途可损耗**;复数 $n$ 因为"往前走"了,才有沿途碰撞生热的机会。

> **镜子比喻**:理想镜子(纯虚 $n$)把光全弹回、自己不发烫;发烫的镜子(复数 $n$)是因为吸收了一部分光变成热。纯虚 $n$ 就是那面**不发烫的理想镜子**——反光但不吸能。能量守恒也印证:进不去、又没变热,就只能原路反射。

**一句话**:"进不去"有两种死法——被弹回(纯虚 $n$,反射,不耗能)vs 被吃掉变热(复数虚部,吸收,耗能),区别就在有没有碰撞 $\nu$。

#### 追问:为什么"幅度掉到零 + 能量守恒"结果是反射?

这里有个看似矛盾:"幅度掉到零"听着像能量没了,结论却是"全反射(能量一点没少)"。逻辑链如下。

**第一步:破误解——"幅度小"不等于"能量没了"。** 能量不会消失(能量守恒),所以真问题是:能量**跑哪去了**?打向电离层的能量只有三个出口:

```
入射能量 = 100
   ┌──────────┬────────────┬────────────┐
   │ 穿透过去   │ 变成热(吸收)│ 反射回来    │
   └──────────┴────────────┴────────────┘
```

**第二步:认清纯虚 $n$ 的"幅度小"是哪一种。** 它不是"波往里走、边走边被吃",而是**根本没往里走**——这种衰减叫**倏逝波(evanescent wave)**:场只在界面附近渗极薄一层就没了,不是往前传播的波(纯虚 $n$ 无振荡项,见上表)。

```
倏逝波:  ██▆▂_          贴着界面探头一下就没,没有能量往深处流
```

**第三步:逐个排除三个出口。** 设入射能量 100:

| 去向 | 纯虚 $n$ 时 | 为什么 |
|------|------------|--------|
| 穿透过去 | **0** | 波进不去(倏逝,只渗极薄一层) |
| 变成热(吸收) | **0** | 无碰撞 $\nu$,没有生热机制;波也没真往里走,无"沿途"可损耗 |
| 反射回来 | **100** | 能量守恒,前两个都是 0,剩下全归这里 |

$$100 = \underbrace{0}_{\text{穿透}} + \underbrace{0}_{\text{吸收}} + \underbrace{R}_{\text{反射}} \;\Rightarrow\; R = 100\%$$

**第四步:界面处"幅度还在"怎么自洽?** 界面(x=0)看到的场 = **入射波 + 反射波的叠加**,不是波进去了:

```
入射 →→→→→ ┃界面        里面:██▆▂_(倏逝,渗一点就没)
反射 ←←←←← ┃            没有能量流进深处 → 净效果:界面完整掉头
```

**逻辑链收束**:

$$\underbrace{f<f_p}_{n\text{ 纯虚}} \Rightarrow \underbrace{\text{波进不去(倏逝、幅度速降到0)}}_{\text{穿透}=0} + \underbrace{\text{无碰撞}}_{\text{吸收}=0} \xRightarrow{\text{能量守恒}} \text{反射}=100\%$$

> **核心**:"幅度掉到零"只说明**波没能穿进去**;能量不会消失,既没穿透又没变热,能量守恒就把它全部"逼"成反射。反射不是因为幅度小,而是"幅度小(进不去)+ 不吸收"两个条件**加上能量守恒共同推出**的唯一出路。这正是理想镜子:不透、不烫,只能全反光。

### 6.4 虚部管吸收:$n$ 为什么会"多出虚部"

6.1 那个公式**假设电子自由振荡、不撞任何东西**。真实电离层里有中性气体分子,电子振荡时会**撞上它们**,把能量变成热——这就是碰撞,用**碰撞频率 $\nu$**(每秒撞几次)描述。

把碰撞加进公式,分母里冒出虚数 $i$:

$$n^2 = 1 - \frac{f_p^2}{f^2 - i\,f\nu}$$

分母带了 $i$,算出来的 $n$ 就是个**复数**:$n = n_{\text{实}} + i\,n_{\text{虚}}$。把它代回传播因子 $e^{i\,n\,kx}$,拆成两半:

$$e^{i(n_{\text{实}} + i\,n_{\text{虚}})kx} = \underbrace{e^{i\,n_{\text{实}}kx}}_{\text{振荡:正常传播}} \cdot \underbrace{e^{-\,n_{\text{虚}}kx}}_{\text{衰减:能量被吸收}}$$

| 因子 | 由 $n$ 的哪部分决定 | 物理效果 |
|------|-------------------|----------|
| $e^{i\,n_{\text{实}}kx}$ | **实部** | 波照常振荡传播(相位、拐弯) |
| $e^{-\,n_{\text{虚}}kx}$ | **虚部** | 幅度随深入距离**指数衰减** = **吸收** |

终端可读版:

```
只有实部:  ∿∿∿∿∿∿∿∿      幅度不变,纯传播
有虚部:    ∿∿∿⌒⌒‿‿___     一边传一边越来越弱 = 被吸收
```

> **虚部管吸收一句话**:碰撞($\nu$)让 $n$ 多出虚部,虚部对应传播中的指数衰减,衰减掉的能量变成热 = 吸收。没有碰撞($\nu=0$)就没虚部,就不吸收。

### 6.5 那个 $e^{inkx}$ 到底是什么?$x$ 又是什么?

前面反复出现的因子 $e^{i\,n\,k\,x}$ 不是凭空的,它是**波的完整表达式(波函数)里"只看空间"的那一半**。

**波的完整等式**:一列沿 $x$ 方向传播的波,场强(电场)随位置 $x$ 和时间 $t$ 变化:

$$E(x,t) = E_0 \cdot e^{i(kx - \omega t)}$$

| 符号 | 含义 | 单位 |
|------|------|------|
| $E(x,t)$ | 位置 $x$、时刻 $t$ 处的场强 | V/m |
| $E_0$ | 初始振幅(源头有多强) | V/m |
| $x$ | 波传播的距离(深入介质多远) | m |
| $t$ | 时间 | s |
| $k$ | 波数($=2\pi/\lambda=2\pi f/c$,每米多少个相位周期) | 1/m |
| $\omega$ | 角频率($=2\pi f$) | 1/s |

**关键:$x$ 是自变量,不等于某个式子。** 它是你输入的"位置/传播了多远",不是被求解的未知量。你想知道"波走到 $x$ 处什么样",把那个 $x$ 代进去即可。

**$e^{inkx}$ 怎么冒出来的**:真空里波数是 $k$;**进入介质后波数要乘折射率**,变成 $nk$。于是介质里的波是 $E_0 e^{i(nkx-\omega t)}$,把随时间振荡的 $e^{-i\omega t}$ 拎出去(它只管时间、与衰减无关),剩下管"随距离 $x$ 怎么变"的就是 $E_0\,e^{i\,n\,k\,x}$。

**真正有等式的,是"波在 $x$ 处的幅度"**:

$$\boxed{\ \text{幅度}(x) = E_0 \cdot e^{-\,n_{\text{虚}}\,k\,x}\ }$$

三种情况随 $x$ 增大的表现:

| $n$ 的性质 | 频率条件 | 幅度随 $x$ | 结果 |
|-----------|----------|-----------|------|
| 实数($n_{\text{虚}}=0$) | $f>f_p$ | $=E_0$ 不变 | **穿透** |
| 复数($n_{\text{虚}}>0$) | 有碰撞 | 指数衰减 $e^{-n_{\text{虚}}kx}$ | **吸收** |
| 纯虚($n^2<0$) | $f<f_p$ | 极快衰减、根本进不去 | **反射** |

终端可读版(横轴是深入距离 $x$):

```
x=0 ────────────────────→ x 增大
实数 n :  ∿∿∿∿∿∿∿∿∿∿∿∿∿∿     幅度恒定 → 穿透
复数 n :  ∿∿∿⌒⌒‿‿·····____     边传边衰减 → 吸收
纯虚 n :  █▇▆▅▃▂▁__________     一进去就掉到 0 → 反射
```

> 一句话:**$x$ 是"你走了多远",$e^{-n_{\text{虚}}kx}$ 告诉你"走到那儿还剩多少能量"。** 虚部越大、走得越深($x$ 越大),剩得越少——这就是吸收随距离累积的数学表达;而 $n$ 变纯虚时衰减快到进不去,就成了反射。

### 6.6 收束:一个复数 $n$ 的两副面孔

| | 由 $n$ 的 | 数学表现 | 物理效果 | 何时显著 |
|---|---------|---------|---------|---------|
| **反射** | 整个 $n$ 变虚 | $n^2<0$,传播因子变指数衰减、波进不去 | 波被弹回地面 | $f<f_p$(频率低于临界) |
| **吸收** | 虚部 | 传播因子多个 $e^{-n_{\text{虚}}kx}$ | 波幅度沿路衰减、能量变热 | 有碰撞时(D 层白天最强) |

判据浓缩:

$$f < f_p \ \Rightarrow\ n^2<0 \ \Rightarrow\ \text{反射}\,;\qquad \nu \neq 0 \ \Rightarrow\ \operatorname{Im}(n)\neq 0 \ \Rightarrow\ \text{吸收}$$

终端可读版:

```
f < fp        → n 变虚   → 波进不去 → 弹回     = 反射
有碰撞 ν≠0    → n 有虚部 → 沿路衰减 → 变成热   = 吸收
```

> 所以"反射"和"吸收"不是两个独立机制,而是**同一个折射率 $n$ 的两副面孔**:实部管弯折/反射,虚部管衰减/吸收。含地磁场的完整形式是 **Appleton–Hartree 方程**,是电离层传播的总方程。

---

## 七、不用记公式的记忆版

```
反射:f 太高 → 穿透飞走      → 上限 MUF = fc / cosθ
吸收:f 太低 → 被 D 层啃光   → 吸收 ∝ 1/f²(频率越低越惨)
选频:卡在 MUF 稍下方最舒服(高频少吸收,又不超上限)
昼夜:白天有 D 层(吸收大),夜里 D 层没(低频复活)
```

---

## 八、与前面知识的关联

| 本篇概念 | 关联到 |
|----------|--------|
| 临界频率 $f_c$、$f>f_c$ 穿透 | 《波长频率与频段》短波 vs VHF/UHF 的传播差异 |
| MUF 选频、频率越高吸收越小 | 《通信模式与调制》CW/SSB 在短波各波段的分布 |
| 吸收导致信号忽强忽弱 | 《通联实务》QSB(信号衰落)、RST 报告的强度变化 |
| 昼夜/太阳影响 | 短波通联要"看时段选频段" |

---

## 待深入话题

- 电离层的昼夜变化、季节变化、11 年太阳黑子周期对 MUF 的影响
- 多跳传播(multi-hop)与跳距、静区(skip zone)
- 灰线传播(grayline)——日出日落线附近的远程增强
- Appleton–Hartree 方程完整形式(含地磁场,产生寻常波/非常波分裂)
