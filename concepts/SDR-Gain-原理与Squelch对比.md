# SDR 中的 Gain：原理、位置与 Squelch 的关系

> 整理自 Mavis 助手对话内容。覆盖：
> 1. Gain 是干什么的
> 2. Gain 的原理
> 3. Gain 在整个流程中的位置（与 Squelch 对比）
> 4. Gain 与 Squelch 的关系

---

## 目录

1. [Gain 是干什么的](#gain-是干什么的)
2. [Gain 的原理](#gain-的原理)
3. [Gain 在整个流程中的位置](#gain-在整个流程中的位置)
4. [Gain 与 Squelch 的关系](#gain-与-squelch-的关系)
5. [实战指南](#实战指南)
6. [常见误区](#常见误区)

---

## Gain 是干什么的

**Gain（增益）= SDR 内部对信号的放大倍数。**

直观理解：

```
你听到的信号有多强 = 信号本身的强度 × Gain
```

**Gain 让你 SDR 内部"看到"更大幅度的信号**。但**不改变信号本身**（频率、原始幅度都没变）。

### 跟其他概念的对比

| 概念 | 改变什么 | 比喻 |
|------|---------|------|
| **Frequency** | 信号的位置（波长） | 换歌曲（不同频率） |
| **Power** | 信号本身的强度 | 离发射塔远近 |
| **Bandwidth** | 解调器取的宽度 | 窗户大小 |
| **Gain** | **SDR 内部放大** | **音响音量旋钮** |
| **Squelch** | 音频门开/关 | 听不听得到 |

**Gain 是"放大器旋钮"，不是"信号本身"**。

---

## Gain 的原理

### 基本概念

**Gain 是一个比值**，告诉你"输出比输入大多少倍"。

```
输出 = 输入 × Gain
```

用 dB 表示（因为是比值）：

```
Gain(dB) = 10 × log10(P_out / P_in)
```

**几个关键参考点**：

| Gain (dB) | 放大倍数 | 实际意义 |
|----------|---------|---------|
| **0 dB** | 1× | 不放大 |
| **+3 dB** | 2× | 翻倍 |
| **+10 dB** | 10× | 增大 10 倍 |
| **+20 dB** | 100× | 增大 100 倍 |
| **+30 dB** | 1000× | 增大 1000 倍 |
| **+50 dB** | 100000× | 增大 10 万倍（RTL-SDR v4 最大） |

### 三种 Gain 类型

| 类型 | 物理位置 | 干啥 | 你需要关心吗 |
|------|---------|------|------------|
| **Tuner Gain** | SDR 内置 RF 芯片 | 主放大，0-50 dB 可调 | **是（最重要）** |
| **LNA 增益** | 外置硬件，天线旁 | 在天线旁放大，加钱买 | 进阶才考虑 |
| **Software Gain** | SDR++ 数字后处理 | 仅显示用，**不改善 SNR** | 不用管 |

### 物理实现：放大器

SDR 内部的 Gain 是用**晶体管放大器**实现的：

```
输入小信号 → [晶体管放大电路] → 输出大信号
              ↑
         电源供电决定
         最大输出上限
```

**关键限制**：电源电压有上限（USB 5V），所以放大器输出有最大值。**超过会发生削波**。

### 削波（Clipping）

**如果 Gain 太大，信号放大后会超过 ADC 量程**。ADC 没办法表示"无限大"的数字，只能表示到自己的最大值（比如 16-bit ADC 最大值 = 32767）。

**超出部分被切掉**：

```
削波前（正常正弦波）：
           ╱╲
          ╱  ╲
       ╱─╱    ╲─╱
     ╱─╱        ╲─╱
────╱──────────────╲────

削波后（Gain 太高）：
       ┌──────────┐  ← 顶部被切平
       │          │
       │          │
──────┘          └──────
```

### 频谱上的"肩膀"现象

**削波在频谱上表现就是肩膀**：

```
正常（Gain 合适）：           削波（Gain 太高）：
   │   ╱╲                       │  ┌──────┐
   │  ╱  ╲                      │ ╱        ╲
   │ ╱    ╲                     │╱          ╲
───┼╱──────╲────         ─────┼─────────────\────
   │ 凸起干净                  │ 平顶 + 肩膀
   └──────────                └──────────────
                              ↑
                          看到肩膀 = 降 Gain
```

**这是 SDR 调参最重要的视觉信号**。

---

## Gain 在整个流程中的位置

### 完整信号链路

```
天线
  ↓
[LNA]（可选外置放大）
  ↓
[Tuner（带 Gain）]  ← Gain 在这里！
  ↓
ADC（模数转换）
  ↓
IQ 采样（数字信号）
  ↓
通道滤波（Bandwidth）
  ↓
频率搬移
  ↓
解调器
  ↓
音频处理
  ↓
[SQUELCH 门]  ← Squelch 在这里！
  ↓
音频输出
```

**关键对比**：

| 参数 | 在链路中的位置 | 作用对象 |
|------|--------------|---------|
| **Gain** | **ADC 之前**（RF 域） | 改变信号的模拟幅度 |
| **Squelch** | **解调之后**（音频域） | 决定音频能不能通过 |

### 位置差异的本质

```
Gain 在 ADC 之前：
  - 处理的还是模拟信号
  - 影响进入 ADC 的电平
  - 可以让弱信号变得"可被 ADC 测量"
  - 调太大会削波（物理限制）

Squelch 在解调之后：
  - 处理的已经是音频数字信号
  - 不影响信号本身
  - 只是决定这个音频通不通过
  - 永远不会削波（只是开/关）
```

### 时序对比

```
信号流过链路：

         Gain          Squelch
          │               │
时间      ↓               ↓
   t1: 信号弱 ───→ Gain 放大 ───→ 进入 ADC ───→ 解调 ───→ Squelch 看："信号弱，开门" ───→ 🔇 静音
   t2: 信号强 ───→ Gain 放大 ───→ 进入 ADC ───→ 解调 ───→ Squelch 看："信号强，开门" ───→ 🔊 出声
   t3: 信号弱 ───→ Gain 放大 ───→ 进入 ADC ───→ 解调 ───→ Squelch 看："信号弱，开门" ───→ 🔇 静音
```

**Gain 在前面改变信号，Squelch 在后面看结果**。

### 关键差异表

| | Gain | Squelch |
|---|------|---------|
| **位置** | ADC **之前** | 解调 **之后** |
| **作用** | 改变信号大小 | 决定音频通不通 |
| **会不会失真** | 会（削波/肩膀） | 不会（只是开关） |
| **可不可以为 0** | 可以（0 dB = 不放大） | 可以（关闭 = 永远开门） |
| **最大值限制** | ADC 量程 | 无 |
| **影响谁** | 影响后面所有环节 | 只影响音频输出 |

---

## Gain 与 Squelch 的关系

### 数学关系

**Squelch 测的是"Gain 之后"的功率**：

```
Squelch 看到的功率 = 天线收到功率 + Gain
```

**所以 Squelch 阈值必须配合 Gain 看**。

### 配对使用

**调参顺序**（**先 Gain，后 Squelch**）：

```
第一步：调 Gain（找甜点）
   - 拉到最大
   - 看频谱
   - 慢慢降 → 肩膀消失 → 锁定
   
第二步：调 Squelch（在 Gain 之后）
   - 先关掉听 10-30 秒
   - 慢慢推阈值 → 沙沙声消失
   - 锁定
```

**反了就麻烦**：
- 先调 Squelch 再调 Gain → Squelch 阈值全错

### 配对实操速查

| Gain | Squelch 阈值 | 适合场景 |
|------|------------|---------|
| **拉满（49 dB）** | 设高一点（-100 dB 左右） | ADS-B 等弱信号 |
| **中等（30 dB）** | 中等（-120 dB） | ATC、HAM 中继 |
| **偏低（10 dB）** | 设低一点（-130 dB） | FM 广播近场 |

### 协同关系详解

```
天线信号弱（-110 dBm）
   ↓
不加 Gain：Squelch 测到 -110 dBm < 阈值 → 关门
加 +30 dB Gain：Squelch 测到 -80 dBm > 阈值 → 开门！
                ↑
          Gain 把"看不见"变成"看得见"
```

**Gain 让 Squelch 能"看到"原本看不到的弱信号**。

### 但反过来：Gain 调太高也不行

```
天线信号 -30 dBm（强！）
   ↓
不加 Gain：Squelch 测到 -30 dBm > 阈值 → 开门 ✓
加 +30 dB Gain：信号变 0 dBm → ADC 削波 → 失真 → Squelch 测到乱七八糟的值
```

**强信号加太多 Gain 反而坏事**。

### AGC（自动增益控制）会破坏这一切

**AGC = SDR 自己不断调 Gain**。

**为什么跟 Squelch 冲突**：
- Squelch 测的是**当前功率 vs 阈值**
- AGC 让当前功率**永远在 AGC 想要的范围**
- Squelch 看到的**永远是 AGC 调过的结果**
- Squelch 阈值**失去意义**

**结论：玩 SDR 必须关 AGC**。

---

## 实战指南

### 调 Gain 的"肩膀法则"

```
1. 把 Gain 拉到最大
2. 看频谱
3. 慢慢降 Gain
4. 找到"肩膀"刚好消失的临界点
5. 这就是甜点
6. 然后才调 Squelch
```

### 典型 Gain 配置

| 场景 | Gain 推荐 |
|------|---------|
| FM 广播近场 | 0-15 dB |
| 民航 ATC | 20-35 dB |
| HAM 2m/70cm 中继 | 20-30 dB |
| 出租车/对讲 | 25-35 dB |
| 气象卫星 | 30-40 dB |
| ADS-B 飞机 | 35-49 dB（拉满） |

### 今晚 ATC 的推荐配置

按你普陀到虹桥 10-15 km 的情况：

```
1. 关 AGC          ← 必做
2. Mode: AM
3. Bandwidth: 6 kHz
4. Gain: 30 dB 起步（看频谱肩膀再微调）
5. Squelch: 先关掉，听到东西后调到 -120 dB 左右
6. 频率: 121.600（虹桥地面）
```

---

## 常见误区

### ❌ "Gain 越大信号越强"

不对。Gain 改变的是 SDR 内部**看到的**信号大小，不是信号**真实**强度。**真实强度取决于发射功率和距离**。

### ❌ "Gain 和音量一样"

不对。音量是音频的放大，**在数字处理之后**。Gain 是在 **RF 域**，在 ADC 之前。两者完全不同。

### ❌ "调高 Gain 一定能收到更多信号"

不对。调太高会削波失真，**反而收不到**。**甜点才是最佳的**。

### ❌ "AGC 开着更省事"

不对。AGC 跟 Squelch 冲突，**让 Squelch 失效**。**永远关 AGC**。

### ❌ "Squelch 跟 Gain 没关系"

不对。Squelch 测的是 **Gain 之后的功率**。**调了 Gain 必须重新调 Squelch**。

### ❌ "信号弱就调高 Gain"

有时候对有时候错。**如果信号弱到 -120 dBm**（比噪声底还低），调高 Gain 也只能抬高噪声。**真正解决弱信号需要 LNA 或者更好的天线**。

---

## 一句话总结

> **Gain = SDR 内部 RF 放大器倍数，0-50 dB 可调。**
> **位于 ADC 之前（RF 域），改变信号进入 ADC 的幅度。**
> **Squelch 位于解调之后（音频域），决定音频通不通。**
> **两者必须配合：先调 Gain（找甜点）→ 再调 Squelch（设阈值）→ 必须关 AGC。**
> **Gain 太高会削波（频谱肩膀），太低会听不到。**

---

*本文档由 Mavis 整理，如有错误欢迎指正。*
